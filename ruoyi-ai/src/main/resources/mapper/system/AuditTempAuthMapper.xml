<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.AuditTempAuthMapper">
    <resultMap id="Result" type="AuditTempAuth">
        <id property="id" column="id"/><result property="userId" column="user_id"/>
        <result property="projectId" column="project_id"/><result property="authType" column="auth_type"/>
        <result property="startDate" column="start_date"/><result property="expireDate" column="expire_date"/>
        <result property="status" column="status"/><result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="userName" column="user_name"/><result property="projectName" column="project_name"/>
    </resultMap>
    <select id="selectList" resultMap="Result">SELECT a.*,u.user_name,p.project_name FROM audit_temp_auth a
        LEFT JOIN sys_user u ON u.user_id=a.user_id LEFT JOIN audit_project p ON p.id=a.project_id
        <where><if test="status!=null">AND a.status=#{status}</if><if test="userId!=null">AND a.user_id=#{userId}</if></where>
        ORDER BY a.create_time DESC</select>
    <select id="selectByUserId" resultMap="Result">SELECT a.*,p.project_name FROM audit_temp_auth a
        LEFT JOIN audit_project p ON p.id=a.project_id WHERE a.user_id=#{userId} AND a.status=1 AND a.expire_date>=CURRENT_DATE</select>
    <insert id="insert" parameterType="AuditTempAuth" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_temp_auth(user_id,project_id,auth_type,start_date,expire_date,status,create_by,create_time)
        VALUES(#{userId},#{projectId},#{authType},#{startDate}::date,#{expireDate}::date,1,#{createBy},now())</insert>
    <update id="update" parameterType="AuditTempAuth">UPDATE audit_temp_auth SET status=#{status} WHERE id=#{id}</update>
    <update id="revokeExpired">UPDATE audit_temp_auth SET status=0 WHERE status=1 AND expire_date &lt; CURRENT_DATE</update>
    <delete id="deleteById">DELETE FROM audit_temp_auth WHERE id=#{id}</delete>
</mapper>
