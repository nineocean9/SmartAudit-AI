<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.AuditPlanAttachMapper">
    <resultMap id="Result" type="AuditPlanAttachment">
        <id property="id" column="id"/><result property="planId" column="plan_id"/>
        <result property="fileName" column="file_name"/><result property="filePath" column="file_path"/>
        <result property="fileType" column="file_type"/><result property="fileSize" column="file_size"/>
        <result property="attachmentType" column="attachment_type"/>
        <result property="createBy" column="create_by"/><result property="createTime" column="create_time"/>
    </resultMap>
    <select id="selectByPlanId" resultMap="Result">SELECT * FROM audit_plan_attachment WHERE plan_id=#{planId} ORDER BY create_time DESC</select>
    <select id="selectById" resultMap="Result">SELECT * FROM audit_plan_attachment WHERE id=#{id}</select>
    <insert id="insert" parameterType="AuditPlanAttachment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_plan_attachment(plan_id,file_name,file_path,file_type,file_size,attachment_type,create_by,create_time)
        VALUES(#{planId},#{fileName},#{filePath},#{fileType},#{fileSize},#{attachmentType},#{createBy},now())
    </insert>
    <delete id="deleteById">DELETE FROM audit_plan_attachment WHERE id=#{id}</delete>
    <delete id="deleteByPlanId">DELETE FROM audit_plan_attachment WHERE plan_id=#{planId}</delete>
</mapper>
